import React from "react";
import Tasks from "../../Components/Tasks";

const TaskList = (props) => {
  const taskListArray = props.array;
  console.log(taskListArray);
  var task1 = "Python";
  var task2 = "Psndsnddddn";
  // return taskListArray.length > 0
  //   ? taskListArray?.map((task) => <Tasks notes={task.newtask} />)
  //   : "";
};

export default TaskList;
